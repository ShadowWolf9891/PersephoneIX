using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// The decorator abstract class makes it so that decoraters can be grouped together.
/// For example, it can be made so that they only have one child.
/// </summary>
public abstract class BTDecorator : BTNode
{
    
}
